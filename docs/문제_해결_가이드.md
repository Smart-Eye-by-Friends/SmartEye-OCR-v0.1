# 🔧 SmartEye v0.4 - 문제 해결 가이드

## 📋 개요

SmartEye v0.4 시스템에서 발생할 수 있는 일반적인 문제들과 해결 방법을 정리한 가이드입니다.

## 🚨 API 파라미터 관련 문제

### ❌ 문제: "Required request parameter 'modelChoice' is not present"

**원인**: 필수 파라미터 `modelChoice`가 누락되었습니다.

**잘못된 사용법:**
```bash
curl -X POST http://localhost:8080/api/document/analyze \
  -F "image=@test_homework_image.jpg" \
  -F "enableOCR=true" # <-- 존재하지 않는 파라미터
```

**✅ 올바른 해결법:**
```bash
curl -X POST http://localhost:8080/api/document/analyze \
  -F "image=@test_homework_image.jpg" \
  -F "modelChoice=SmartEyeSsen"
```

### ❌ 문제: "Required part 'image' is not present"

**원인**: 파일 파라미터 이름이 `image`가 아닙니다.

**잘못된 사용법:**
```bash
curl -X POST ... -F "file=@test.jpg" # 'file'이 아니라 'image'여야 합니다.
```

**✅ 올바른 해결법:**
```bash
curl -X POST ... -F "image=@test.jpg" ...
```

## 🔧 서비스 연결 문제

### ❌ 문제: "Connection refused" (8080, 8001, 5433 등)

**원인**: 관련 서비스 컨테이너가 실행되지 않았거나 오류로 중지된 상태입니다.

**진단 방법:**
```bash
# 1. 전체 서비스 상태 확인
./manage.sh status

# 2. 특정 서비스 로그 확인 (예: backend)
./manage.sh logs smarteye-backend
```

**✅ 해결법:**
```bash
# 1. 전체 시스템 재시작
./manage.sh start

# 2. 특정 서비스만 재시작 (예: backend)
./manage.sh restart smarteye-backend
```

## 🗃️ 데이터베이스 문제

### ❌ 문제: "password authentication failed"

**원인**: 데이터베이스 인증 정보가 올바르지 않습니다.

**✅ 해결법:**
`Backend/.env` 파일의 `DB_USERNAME`과 `DB_PASSWORD`가 `docker-compose.yml` 파일의 `POSTGRES_USER`, `POSTGRES_PASSWORD`와 일치하는지 확인하세요.

### ❌ 문제: 데이터베이스 테이블이 생성되지 않음

**원인**: `init.sql` 스크립트가 실행되지 않았거나, 볼륨 문제일 수 있습니다.

**✅ 해결법:**
```bash
# 1. 시스템을 중지하고 데이터베이스 볼륨까지 삭제합니다.
./manage.sh stop
# (볼륨 삭제 여부를 묻는 메시지에 'y'를 입력)

# 2. 시스템을 다시 시작하여 DB를 새로 생성합니다.
./manage.sh start
```

## 📁 파일 처리 문제

### ❌ 문제: "Maximum upload size exceeded"

**원인**: 업로드한 파일 크기가 50MB 제한을 초과했습니다.

**✅ 해결법:**
1.  파일 크기를 50MB 이하로 줄입니다.
2.  필요 시 `Backend/smarteye-backend/src/main/resources/application.yml` 파일에서 `spring.servlet.multipart.max-file-size` 값을 수정하고, `./manage.sh rebuild smarteye-backend`로 서비스를 재빌드합니다.

## ⚡ 성능 및 메모리 문제

### ❌ 문제: "java.lang.OutOfMemoryError"

**원인**: JVM 힙 메모리가 부족합니다.

**✅ 해결법:**
`Backend/docker-compose.yml` 파일에서 `smarteye-backend` 서비스의 `deploy.resources.limits.memory` 값을 `2g`에서 `4g` 등으로 늘린 후, `./manage.sh restart smarteye-backend`로 재시작합니다.

### ❌ 문제: 분석 처리 시간이 너무 오래 걸림

**원인**: 대용량 파일 또는 복잡한 레이아웃으로 인해 AI 모델 처리 시간이 길어질 수 있습니다.

**✅ 해결법:**
- **정상적인 현상일 수 있습니다.** 특히 첫 분석 시에는 AI 모델을 다운로드하므로 시간이 더 오래 걸립니다.
- 문제가 지속되면 `./manage.sh logs lam-service`로 AI 서비스의 로그를 확인하여 병목 현상을 파악하세요.

## 🔄 시스템 전체 재시작

**원인을 알 수 없는 문제가 발생하여 시스템 전체를 초기화하고 싶을 때 사용합니다.**

```bash
# 1. 모든 컨테이너 중지 및 데이터 볼륨 삭제
./manage.sh stop
# (볼륨 삭제 여부를 묻는 메시지에 'y'를 입력)

# 2. 모든 서비스 강제 재빌드 후 시작
./manage.sh rebuild all

# 3. 시스템 상태 확인
./manage.sh status
```

## 📞 추가 도움

문제가 지속될 경우, 다음 문서들을 참고하세요:
- `개발자_가이드.md`
- `API_테스팅_가이드.md`