# 🏗️ SmartEye OCR 시스템 아키텍처
**AI 기반 학습지 OCR 및 구조 분석 시스템**

---

## 📋 목차
1. [시스템 개요](#시스템-개요)
2. [전체 시스템 아키텍처](#전체-시스템-아키텍처)
3. [마이크로서비스 아키텍처](#마이크로서비스-아키텍처)
4. [기술 스택](#기술-스택)
5. [데이터 흐름](#데이터-흐름)
6. [배포 아키텍처](#배포-아키텍처)
7. [보안 및 확장성](#보안-및-확장성)

---

## 🎯 시스템 개요

### 프로젝트 목표
- **AI 기반 학습지 자동 분석**: OCR과 레이아웃 분석을 통한 학습지 디지털화
- **구조화된 문제 추출**: 문제별 분리 및 체계적 정리
- **실시간 웹 서비스**: 사용자 친화적 인터페이스 제공

### 핵심 기능
- 📸 **이미지 업로드 및 전처리**
- 🔍 **레이아웃 분석 (YOLO 기반)**
- 📝 **OCR 텍스트 추출 (Tesseract)**
- 🤖 **AI 기반 문제 분석 (OpenAI GPT)**
- 📊 **구조화된 결과 생성**
- 💾 **결과 저장 및 관리**

---

## 🏛️ 전체 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                      Client Layer                           │
├─────────────────────────────────────────────────────────────┤
│  📱 React Frontend (Port 3000)                            │
│  ├── Components: ImageLoader, ResultTabs, AnalysisProgress │
│  ├── Services: ApiService, TextEditor                     │
│  └── Hooks: useAnalysis, useTextEditor                    │
└─────────────────────────────────────────────────────────────┘
                              │ HTTP/REST API
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   Presentation Layer                        │
├─────────────────────────────────────────────────────────────┤
│  🌐 Nginx Reverse Proxy (Port 80/443)                    │
│  ├── SSL Termination                                      │
│  ├── Load Balancing                                       │
│  └── Static File Serving                                  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   Application Layer                         │
├─────────────────────────────────────────────────────────────┤
│  ☕ Spring Boot Backend (Port 8080)                       │
│  ├── Controllers: Document, Health, Job, User             │
│  ├── Services: LAMClient, OCR, AI, File, PDF             │
│  ├── Security: CORS, Validation, Exception Handling       │
│  └── Async Processing: CompletableFuture, Circuit Breaker │
└─────────────────────────────────────────────────────────────┘
                              │
            ┌─────────────────┼─────────────────┐
            │                 │                 │
            ▼                 ▼                 ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   AI Services   │ │  Data Storage   │ │ External APIs   │
│                 │ │                 │ │                 │
│  🧠 LAM Service │ │ 🗄️ PostgreSQL   │ │ 🤖 OpenAI API   │
│   (Port 8001)   │ │  (Port 5433)    │ │   GPT-4 Turbo   │
│                 │ │                 │ │                 │
│ ├─ YOLO Model   │ │ ├─ User Data    │ │ ├─ Image Analysis│
│ ├─ Layout       │ │ ├─ Job Queue    │ │ ├─ Text Analysis │
│ │   Analysis     │ │ ├─ Results     │ │ └─ Problem      │
│ └─ Structured   │ │ └─ Files       │ │    Analysis     │
│    Generation   │ │                 │ │                 │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

---

## 🔧 마이크로서비스 아키텍처

### 1. Frontend Service (React)
```
📦 React Application
├── 🎨 UI Components
│   ├── ImageLoader.jsx - 이미지 업로드
│   ├── ModelSelector.jsx - AI 모델 선택
│   ├── AnalysisProgress.jsx - 진행 상황 표시
│   ├── ResultTabs.jsx - 결과 탭 네비게이션
│   ├── LayoutTab.jsx - 레이아웃 분석 결과
│   ├── StatsTab.jsx - 분석 통계
│   ├── TextEditorTab.jsx - 텍스트 편집
│   ├── AITab.jsx - AI 분석 결과
│   └── StructuredTab.jsx - 구조화된 문제
├── 🔧 Custom Hooks
│   ├── useAnalysis.js - 분석 로직 관리
│   └── useTextEditor.js - 텍스트 편집 관리
├── 🌐 Services
│   └── apiService.js - REST API 통신
└── 📱 App.jsx - 메인 애플리케이션
```

### 2. Backend Service (Spring Boot)
```
☕ Spring Boot Application
├── 🌐 REST Controllers
│   ├── DocumentAnalysisController - 문서 분석 API
│   ├── DocumentProcessingController - 문서 처리 API
│   ├── BookController - 책 단위 분석
│   ├── HealthController - 시스템 상태
│   ├── JobStatusController - 작업 상태 관리
│   └── UserController - 사용자 관리
├── 🏗️ Service Layer
│   ├── LAMServiceClient - LAM 마이크로서비스 클라이언트
│   ├── OCRService - Tesseract OCR 처리
│   ├── AIDescriptionService - OpenAI API 연동
│   ├── FileService - 파일 업로드/다운로드
│   ├── ImageProcessingService - 이미지 전처리
│   ├── PDFService - PDF 처리
│   ├── BookService - 책 분석 관리
│   ├── AnalysisJobService - 비동기 작업 관리
│   └── DocumentAnalysisDataService - 결과 저장
├── 🗄️ Data Layer
│   ├── Entities - JPA 엔티티
│   ├── Repositories - 데이터 접근
│   └── DTOs - 데이터 전송 객체
├── ⚙️ Configuration
│   ├── WebClientConfig - HTTP 클라이언트 설정
│   ├── WebConfig - CORS 설정
│   └── DatabaseConfig - 데이터베이스 설정
└── 🛡️ Security & Exception
    ├── GlobalExceptionHandler - 예외 처리
    └── Validation - 입력 검증
```

### 3. LAM Service (Python FastAPI)
```
🐍 Python FastAPI Application
├── 🧠 AI Models
│   ├── YOLO - 레이아웃 객체 감지
│   ├── DocLayout-YOLO - 문서 구조 분석
│   └── SmartEyeSsen - 커스텀 모델
├── 📊 Analysis Modules
│   ├── layout_analyzer_enhanced.py - 레이아웃 분석
│   ├── structured_json_generator.py - 구조화 생성
│   └── main.py - FastAPI 엔드포인트
├── 🔄 Processing Pipeline
│   ├── Image Preprocessing - 이미지 전처리
│   ├── Object Detection - 객체 감지
│   ├── Layout Analysis - 레이아웃 분석
│   └── Structured Output - 구조화된 결과
└── 📈 Health Monitoring
    ├── Health Checks - 서비스 상태
    ├── Performance Metrics - 성능 지표
    └── Error Logging - 오류 로깅
```

---

## 🛠️ 기술 스택

### Frontend Stack
```
🎨 Frontend Technologies
├── ⚛️ React 18.2.0 - UI 프레임워크
├── 🌐 Axios 1.6.0 - HTTP 클라이언트
├── 🎪 React Router DOM 6.8.0 - 라우팅
├── 🎯 React Icons 4.12.0 - 아이콘
├── ✏️ TinyMCE React 4.3.2 - 텍스트 에디터
└── 📱 Web Vitals 3.3.2 - 성능 측정
```

### Backend Stack
```
☕ Backend Technologies
├── 🍃 Spring Boot 3.5.5 - 웹 프레임워크
├── 🗄️ Spring Data JPA - 데이터 접근
├── 🌐 Spring WebFlux - 비동기 HTTP 클라이언트
├── ✅ Spring Validation - 입력 검증
├── 📊 Spring Actuator - 모니터링
├── 🔄 Spring Cache - 캐싱
├── 🐘 PostgreSQL - 데이터베이스
├── 🧪 Resilience4j - 서킷 브레이커
├── 📋 OpenAPI 3 - API 문서화
└── 🖼️ Tesseract OCR - 텍스트 추출
```

### AI/ML Stack
```
🧠 AI/ML Technologies
├── 🐍 Python 3.9+ - AI 서비스 언어
├── ⚡ FastAPI - 고성능 웹 프레임워크
├── 🔥 PyTorch - 딥러닝 프레임워크
├── 🤗 HuggingFace - 모델 허브
├── 👁️ YOLO - 객체 감지
├── 📊 OpenCV - 이미지 처리
├── 🔢 NumPy - 수치 계산
├── 📈 Pandas - 데이터 처리
└── 🤖 OpenAI GPT-4 - 자연어 처리
```

### Infrastructure Stack
```
🏗️ Infrastructure Technologies
├── 🐳 Docker - 컨테이너화
├── 🐙 Docker Compose - 오케스트레이션
├── 🌐 Nginx - 리버스 프록시
├── 🐘 PostgreSQL 15 - 관계형 데이터베이스
├── 📁 Volume Mounts - 데이터 영속성
├── 🔗 Bridge Network - 컨테이너 통신
└── 🏥 Health Checks - 서비스 모니터링
```

---

## 🔄 데이터 흐름

### 1. 이미지 업로드 및 분석 프로세스
```
📱 Client                    ☕ Backend                   🐍 LAM Service
   │                           │                           │
   │ 1. 이미지 업로드            │                           │
   ├─────────────────────────► │                           │
   │   POST /api/document/      │                           │
   │   analyze-structured       │                           │
   │                           │                           │
   │                           │ 2. 이미지 저장 & 검증       │
   │                           ├─────────────────────────► │
   │                           │   JobID 생성              │
   │                           │                           │
   │                           │ 3. LAM 서비스 호출         │
   │                           ├─────────────────────────► │
   │                           │   POST /analyze-structured │
   │                           │                           │
   │                           │                           │ 4. YOLO 모델 실행
   │                           │                           ├──────────────►
   │                           │                           │   객체 감지 수행
   │                           │                           │
   │                           │                           │ 5. 레이아웃 분석
   │                           │                           ├──────────────►
   │                           │                           │   구조화된 결과 생성
   │                           │                           │
   │                           │ 6. 분석 결과 반환          │
   │                           │◄─────────────────────────┤
   │                           │   JSON 응답               │
   │                           │                           │
   │                           │ 7. OCR 처리 (Tesseract)   │
   │                           ├─────────────────────────► │
   │                           │   텍스트 추출             │
   │                           │                           │
   │                           │ 8. AI 분석 (OpenAI)       │
   │                           ├─────────────────────────► │
   │                           │   문제 해석 및 설명        │
   │                           │                           │
   │                           │ 9. 데이터베이스 저장       │
   │                           ├─────────────────────────► │
   │                           │   PostgreSQL              │
   │                           │                           │
   │ 10. 최종 결과 응답          │                           │
   │◄─────────────────────────┤                           │
   │    구조화된 JSON 데이터     │                           │
```

### 2. 데이터베이스 스키마
```sql
-- 사용자 관리
Users
├── user_id (PK)
├── username
├── email
├── created_at
└── updated_at

-- 분석 작업 관리
AnalysisJobs
├── job_id (PK)
├── user_id (FK)
├── status (PENDING/IN_PROGRESS/COMPLETED/FAILED)
├── progress (0-100)
├── file_path
├── created_at
└── updated_at

-- 레이아웃 분석 결과
LayoutBlocks
├── block_id (PK)
├── job_id (FK)
├── class_name
├── confidence
├── bbox_coordinates
├── width, height
└── created_at

-- OCR 텍스트 결과
TextBlocks
├── text_id (PK)
├── job_id (FK)
├── block_id (FK)
├── extracted_text
├── confidence
├── language
└── created_at

-- AI 분석 결과
AIDescriptions
├── ai_id (PK)
├── job_id (FK)
├── block_id (FK)
├── description
├── analysis_type
├── confidence
└── created_at
```

---

## 🚀 배포 아키텍처

### Docker Compose 서비스 구성
```yaml
🐳 Docker Services Architecture

├── 🌐 smarteye-nginx (Port 80/443)
│   ├── SSL Termination
│   ├── Reverse Proxy
│   └── Static File Serving
│
├── ☕ smarteye-backend (Port 8080)
│   ├── Memory: 2GB (Limit)
│   ├── Health Check: /api/health
│   ├── Volumes: uploads, static, logs
│   └── Environment: Production Profile
│
├── 🐍 smarteye-lam-service (Port 8001)
│   ├── Memory: 4GB (Limit)
│   ├── Health Check: /health
│   ├── Volumes: models, logs
│   └── Environment: CUDA Support
│
└── 🐘 smarteye-postgres (Port 5433)
    ├── Database: smarteye_db
    ├── Health Check: pg_isready
    ├── Volume: postgres_data
    └── Init Script: schema.sql
```

### 네트워크 구성
```
🔗 Docker Network: smarteye-network (Bridge)

External Access:
├── Frontend: http://localhost:3000 (Development)
├── Nginx: http://localhost:80, https://localhost:443
├── Backend API: http://localhost:8080
├── LAM Service: http://localhost:8001
└── Database: localhost:5433

Internal Communication:
├── nginx → smarteye-backend:8080
├── smarteye-backend → lam-service:8001
├── smarteye-backend → postgres:5432
└── smarteye-backend → external APIs
```

### 볼륨 관리
```
📁 Docker Volumes

Persistent Data:
├── postgres_data/ - 데이터베이스 데이터
├── lam_models/ - AI 모델 파일
├── backend_uploads/ - 업로드된 파일
├── backend_static/ - 정적 파일
└── application_logs/ - 서비스 로그

Mounted Directories:
├── ./nginx/nginx.conf → /etc/nginx/nginx.conf
├── ./nginx/ssl/ → /etc/nginx/ssl/
├── ./Backend/smarteye-backend/logs/ → /app/logs/
└── ./Backend/smarteye-lam-service/logs/ → /app/logs/
```

---

## 🛡️ 보안 및 확장성

### 보안 정책
```
🔒 Security Measures

Web Security:
├── 🌐 CORS Policy - 허용된 도메인만 접근
├── 🛡️ HTTPS/SSL - 암호화된 통신
├── 📝 Input Validation - 악성 입력 차단
├── 🚧 Rate Limiting - DDoS 방지
└── 🔍 File Type Validation - 안전한 파일만 허용

API Security:
├── 🔑 API Key Management - OpenAI API 보안
├── 🏥 Health Check Endpoints - 시스템 모니터링
├── 📊 Request Logging - 접근 기록
├── ⚠️ Error Handling - 정보 노출 방지
└── 🔄 Circuit Breaker - 서비스 보호

Data Security:
├── 🗄️ Database Encryption - 데이터 암호화
├── 🔐 Environment Variables - 민감 정보 보호
├── 📁 File Access Control - 접근 권한 관리
└── 🗑️ Data Retention Policy - 데이터 수명 관리
```

### 확장성 설계
```
📈 Scalability Architecture

Horizontal Scaling:
├── 🔄 Load Balancer - Nginx 다중 백엔드 지원
├── 🐳 Container Orchestration - Docker Swarm/K8s 준비
├── 🗄️ Database Sharding - PostgreSQL 분산 지원
└── 📊 Microservice Independence - 독립적 스케일링

Performance Optimization:
├── ⚡ Async Processing - CompletableFuture 비동기 처리
├── 🔄 Connection Pooling - 데이터베이스 연결 최적화
├── 📦 Caching Strategy - Spring Cache 활용
├── 🖼️ Image Optimization - 자동 이미지 압축
└── 📈 Resource Monitoring - 실시간 성능 추적

AI Model Scaling:
├── 🧠 Model Versioning - 다중 모델 지원
├── 🎯 GPU Acceleration - CUDA 활용 가능
├── 📊 Batch Processing - 대량 처리 지원
└── 🔄 Model Hot-Swapping - 무중단 모델 업데이트
```

### 모니터링 및 로깅
```
📊 Monitoring & Logging

Application Monitoring:
├── 🏥 Health Checks - 자동 서비스 상태 확인
├── 📈 Performance Metrics - CPU, 메모리, 응답시간
├── 📊 Spring Actuator - JVM 및 애플리케이션 지표
└── 🔔 Alert System - 장애 자동 알림

Logging Strategy:
├── 📝 Structured Logging - JSON 형태 로그
├── 📁 Log Rotation - 자동 로그 순환
├── 🔍 Centralized Logging - 중앙집중식 로그 관리
└── 📊 Log Analysis - 패턴 분석 및 인사이트

Error Tracking:
├── ⚠️ Exception Handling - 체계적 예외 처리
├── 🐛 Error Classification - 오류 유형별 분류
├── 📊 Error Rate Monitoring - 오류율 추적
└── 🔄 Automatic Recovery - 자동 복구 메커니즘
```

---

## 📊 시스템 성능 지표

### 처리 성능
- **이미지 분석 시간**: 평균 2-5초 (이미지 크기에 따라)
- **동시 처리 능력**: 최대 3개 작업 병렬 처리
- **메모리 사용량**: Backend 2GB, LAM Service 4GB
- **API 응답 시간**: 평균 < 200ms (분석 요청 제외)

### 지원 규격
- **이미지 형식**: JPG, PNG, GIF
- **최대 파일 크기**: 50MB
- **이미지 해상도**: 자동 최적화 (최대 1920px)
- **언어 지원**: 한국어, 영어 (Tesseract OCR)

---

## 🎯 향후 확장 계획

### 단기 계획 (3개월)
- 📱 모바일 반응형 UI 개선
- 🔍 검색 및 필터링 기능
- 📊 대시보드 및 통계 페이지
- 🔐 사용자 인증 시스템

### 중기 계획 (6개월)
- 🤖 더 정교한 AI 모델 통합
- 📚 배치 처리 (여러 이미지 동시 분석)
- 🌐 다국어 지원 확대
- 📈 고급 분석 리포트

### 장기 계획 (1년)
- ☁️ 클라우드 배포 (AWS/GCP)
- 🔄 실시간 협업 기능
- 📱 모바일 앱 개발
- 🧠 커스텀 AI 모델 훈련

---

*이 문서는 SmartEye OCR 시스템의 현재 아키텍처를 상세히 설명하며, 교수님과 담당자분들의 이해를 돕기 위해 작성되었습니다.*
