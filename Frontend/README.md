# 🎯 SmartEye Frontend - React 18 사용자 인터페이스

**마이크로서비스 아키텍처 기반 학습지 분석 시스템의 모던 React 프론트엔드**

SmartEye 시스템의 사용자 인터페이스로, Java Spring Boot 백엔드와 Python LAM 서비스와 연동하여 AI 기반 학습지 OCR 및 구조 분석 결과를 직관적으로 제공합니다.

## 🚀 주요 기능

### 📤 고급 이미지 업로드
- 드래그 앤 드롭 지원
- 다중 파일 형식 지원: JPG, PNG, GIF, PDF, BMP, TIFF
- 최대 50MB 파일 크기 제한 (백엔드 최적화)
- 실시간 이미지 미리보기 및 압축 옵션
- 클라이언트 측 파일 유효성 검증

### 🧠 AI 모델 선택 (4가지 DocLayout-YOLO 모델)
- **SmartEyeSsen**: 한국어 학습지에 최적화 (권장) - 커스텀 파인튜닝
- **DocStructBench**: 일반적인 문서 구조 분석 - 1024px 해상도
- **DocLayNet-DocSynth**: 복잡한 레이아웃 분석 - DocLayNet + DocSynth300K 사전 훈련
- **DocSynth300K**: 대용량 학습 데이터 기반 - 1600px 고해상도

### 📋 분석 모드 (2가지 파이프라인)
- **기본 분석**: OCR + 레이아웃 분석 + AI 설명
- **구조화된 분석**: TSPM 엔진 기반 문제별 정렬 + CIM 출력 (권장)

### 🤖 OpenAI GPT-4 Vision 연동
- API 키를 통한 고급 이미지 분석
- 그림, 차트, 표에 대한 자연어 설명 자동 생성
- Circuit Breaker 패턴으로 안정성 보장
- 선택적 활성화 (API 키 없이도 OCR + 레이아웃 분석 가능)

### 📊 분석 결과 탭 (5개 탭 구성)
1. **레이아웃 분석**: 33가지 레이아웃 요소의 위치와 신뢰도
2. **분석 통계**: 감지된 요소 개수, 처리 시간, 모델 정보
3. **텍스트 편집**: TinyMCE 기반 OCR 결과 편집 및 포맷팅
4. **AI 설명**: OpenAI GPT-4 Vision으로 생성된 이미지 분석
5. **구조화된 결과**: TSPM 엔진 기반 문제별 정렬 및 JSON 구조

### 💾 고급 저장 기능
- **텍스트 파일**: 일반 텍스트 (.txt) 다운로드
- **워드 문서**: 백엔드 Apache POI 연동 DOCX 생성
- **클립보드 복사**: 브라우저 Clipboard API 활용
- **로컬 스토리지**: 자동 저장 및 세션 복구
- **JSON 다운로드**: 전체 분석 결과 구조화된 데이터

## 🛠️ 기술 스택

### **코어 프레임워크**
- **React 18.2.0** - 최신 Concurrent Features 및 React Hooks
- **React Scripts 5.0.1** - Create React App 최신 버전
- **React Router DOM 6.8.0** - 클라이언트 사이드 라우팅

### **HTTP 통신 & API**
- **Axios 1.6.0** - Promise 기반 HTTP 클라이언트
- **Proxy 설정**: `http://localhost:8080` (Backend 연동)
- **인터셉터**: 요청/응답 로깅 및 에러 처리

### **UI 컴포넌트 & 에디터**
- **TinyMCE React 4.3.2** - WYSIWYG 리치 텍스트 에디터
- **React Icons 4.12.0** - 아이콘 라이브러리
- **CSS Variables & Flexbox** - 반응형 디자인

### **성능 & 최적화**
- **Web Vitals 3.3.2** - Core Web Vitals 측정
- **Code Splitting** - React.lazy() 기반 번들 최적화
- **Memoization** - useCallback, useMemo 활용

## 📁 프로젝트 구조

```
Frontend/
├── public/                  # 정적 파일
│   ├── index.html              # 메인 HTML 템플릿
│   ├── manifest.json           # PWA 매니페스트
│   └── robots.txt              # 검색 엔진 설정
├── src/
│   ├── components/          # React 컴포넌트 (10개)
│   │   ├── ImageLoader.jsx         # 이미지 업로드 & 드래그드롭
│   │   ├── ModelSelector.jsx       # AI 모델 선택 (4가지 모델)
│   │   ├── AnalysisModeSelector.jsx # 분석 모드 선택 (기본/구조화)
│   │   ├── AnalysisProgress.jsx    # 진행률 표시 & 상태 피드백
│   │   ├── ResultTabs.jsx          # 결과 탭 컨테이너 & 네비게이션
│   │   ├── LayoutTab.jsx           # 레이아웃 분석 결과 (33가지 요소)
│   │   ├── StatsTab.jsx            # 분석 통계 & 성능 메트릭
│   │   ├── TextEditorTab.jsx       # TinyMCE 텍스트 편집기
│   │   ├── AITab.jsx               # OpenAI 기반 AI 분석 결과
│   │   └── StructuredTab.jsx       # TSPM 구조화된 분석 결과
│   ├── hooks/               # 커스텀 훅 (2개)
│   │   ├── useAnalysis.js          # 분석 API 호출 & 상태 관리
│   │   └── useTextEditor.js        # 텍스트 편집 & 저장 로직
│   ├── services/            # API 서비스
│   │   └── apiService.js           # Axios 기반 백엔드 통신
│   ├── styles/              # CSS 스타일
│   │   ├── index.css               # 전역 스타일 & CSS Variables
│   │   └── App.css                 # 컴포넌트별 스타일
│   ├── App.jsx              # 메인 앱 컴포넌트
│   └── index.js             # React 엔트리 포인트
├── package.json             # 의존성 & 스크립트
├── Dockerfile               # Docker 컨테이너화
├── nginx.conf               # Nginx 설정 (프로덕션)
└── .env.production          # 환경 변수 설정
```

### 📊 구현 통계
- **React 컴포넌트**: 10개 (재사용 가능한 UI 컴포넌트)
- **커스텀 훅**: 2개 (비즈니스 로직 분리)
- **API 서비스**: 1개 (중앙집중식 HTTP 통신)
- **총 라인 수**: 약 1,500라인 (주석 포함)
- **번들 크기**: ~500KB (gzipped)

## 🔧 설치 및 실행

### 1. 시스템 요구사항

- **Node.js 18+** (React 18 호환성)
- **npm 8+** 또는 **yarn 1.22+**
- **메모리**: 최소 2GB RAM (개발 서버 + 빌드)
- **디스크**: 최소 1GB 여유 공간

### 2. 프로젝트 설치

```bash
# 저장소 클론 후 Frontend 디렉토리로 이동
cd Frontend

# 의존성 설치
npm install

# 또는 Yarn 사용
yarn install
```

### 3. 환경 변수 설정

프로젝트 루트에 `.env` 파일을 생성하고 다음을 추가:

```env
# 백엔드 API 서버 URL
REACT_APP_API_URL=http://localhost:8080

# 선택사항: 개발 모드 설정
REACT_APP_ENV=development

# 선택사항: 디버그 모드
REACT_APP_DEBUG=true
```

### 4. 개발 서버 실행

```bash
# 개발 서버 시작 (백엔드 프록시 포함)
npm start

# 또는 Yarn 사용
yarn start
```

브라우저에서 **http://localhost:3000** 자동 접속
- **프록시 설정**: `http://localhost:8080`으로 API 요청 자동 포워딩
- **핫 리로드**: 파일 변경 시 자동 갱신
- **에러 오버레이**: 개발 중 오류 표시

### 5. 프로덕션 빌드

```bash
# 프로덕션 최적화 빌드
npm run build

# 빌드 결과 확인
ls -la build/

# 정적 파일 서버로 테스트
npx serve -s build -l 3000
```

**빌드 최적화 결과**:
- **번들 크기**: ~500KB (gzipped)
- **코드 스플리팅**: React.lazy() 기반 자동 분할
- **트리 셰이킹**: 사용하지 않는 코드 제거
- **에셋 최적화**: 이미지 압축 및 캐싱 헤더

## 🔌 마이크로서비스 백엔드 연동

### 서비스 아키텍처

SmartEye Frontend는 다음 마이크로서비스들과 통신합니다:

- **🏗️ SmartEye Backend** (Java Spring Boot 3.5.5): `http://localhost:8080`
- **🤖 LAM Service** (Python FastAPI): `http://localhost:8001`
- **🐘 PostgreSQL Database**: `localhost:5433`
- **🌐 Nginx Proxy**: `http://localhost:80` (프로덕션)

### REST API 엔드포인트

**문서 분석 API**:
```javascript
// 📄 기본 분석 (OCR + 레이아웃 + AI 설명)
POST /api/document/analyze

// 🧩 구조화된 분석 (TSPM 엔진 + CIM 출력)
POST /api/document/analyze-structured

// 📊 분석 상태 확인
GET /api/document/analysis/{jobId}/status

// 💾 워드 문서 생성
POST /api/document/save-as-word
```

**시스템 모니터링 API**:
```javascript
// ✅ 전체 시스템 헬스 체크
GET /api/health

// 🐘 데이터베이스 연결 상태
GET /api/health/database

// 🤖 LAM Service 연결 상태
GET /api/health/lam-service

// 📊 시스템 메트릭
GET /actuator/metrics
```

### 요청 형식 및 예제

**이미지 분석 요청**:
```javascript
const analysisRequest = async (imageFile, options) => {
  const formData = new FormData();

  // 필수 파라미터
  formData.append('image', imageFile);
  formData.append('modelChoice', options.model || 'SmartEyeSsen');

  // 선택사항 파라미터
  if (options.apiKey) formData.append('apiKey', options.apiKey);
  if (options.analysisMode) formData.append('analysisMode', options.analysisMode);

  const response = await axios.post('/api/document/analyze', formData, {
    headers: { 'Content-Type': 'multipart/form-data' },
    timeout: 300000, // 5분 타임아웃
    onUploadProgress: (progressEvent) => {
      const progress = Math.round((progressEvent.loaded * 100) / progressEvent.total);
      updateProgress(progress);
    }
  });

  return response.data;
};
```

### 응답 형식 및 데이터 구조

**성공 응답**:
```javascript
{
  "success": true,
  "jobId": "job-12345-abcde",
  "layout_image_url": "/static/layout_viz_20240917_143052.png",
  "json_url": "/static/analysis_result_20240917_143052.json",

  // 📊 분석 통계 정보
  "stats": {
    "processing_time": 15.432,
    "detected_elements": 28,
    "model_used": "SmartEyeSsen",
    "confidence_avg": 0.847,
    "page_count": 1
  },

  // 📝 OCR 텍스트 결과 (33가지 레이아웃 요소)
  "ocr_results": [
    {
      "id": "block_1",
      "text": "1. 다음 중 올바른 것은?",
      "confidence": 0.95,
      "bbox": [100, 150, 300, 200],
      "label": "question_number"
    }
  ],

  // 🤖 AI 생성 설명 (OpenAI GPT-4 Vision)
  "ai_results": [
    {
      "image_section": "chart_1",
      "description": "막대그래프로 표현된 월별 판매량 데이터",
      "confidence": 0.92
    }
  ],

  // 📄 포맷된 전체 텍스트
  "formatted_text": "1. 다음 중 올바른 것은?\n① 선택지 1\n② 선택지 2...",

  // 🧩 구조화된 분석 결과 (TSPM 엔진)
  "structured_result": {
    "problems": [
      {
        "number": 1,
        "question_text": "다음 중 올바른 것은?",
        "choices": ["선택지 1", "선택지 2", "선택지 3", "선택지 4"],
        "answer": "③",
        "explanation": "정답 해설 내용..."
      }
    ],
    "metadata": {
      "total_problems": 10,
      "difficulty_level": "medium",
      "subject": "수학"
    }
  }
}
```

**에러 응답**:
```javascript
{
  "success": false,
  "error": {
    "code": "ANALYSIS_FAILED",
    "message": "이미지 분석 중 오류가 발생했습니다",
    "details": "LAM Service 연결 실패",
    "timestamp": "2024-09-17T14:30:52Z"
  }
}
```

## 🎨 UI/UX 특징

### 반응형 디자인
- 데스크톱, 태블릿, 모바일 지원
- CSS Grid 및 Flexbox 활용
- 모던 CSS Variables 사용

### 사용자 경험
- 직관적인 드래그 앤 드롭 인터페이스
- 실시간 진행률 표시
- 탭 기반 결과 표시
- 자동 저장 기능

### 접근성
- 키보드 네비게이션 지원
- 시맨틱 HTML 구조
- 명확한 레이블링
- 고대비 색상 사용

## 🔍 주요 컴포넌트 설명

### `useAnalysis` 훅
- 분석 상태 관리
- API 호출 및 오류 처리
- 진행률 시뮬레이션

### `useTextEditor` 훅
- 텍스트 편집 상태 관리
- 로컬 스토리지 자동 저장
- 다양한 내보내기 기능

### `apiService`
- Axios 기반 HTTP 클라이언트
- 요청/응답 인터셉터
- 파일 업로드 유효성 검사

## 🚨 오류 처리

### 클라이언트 측 검증
- 파일 크기 제한 (10MB)
- 파일 형식 검증 (JPG, PNG, GIF)
- 네트워크 연결 상태 확인

### 서버 오류 대응
- HTTP 상태 코드별 메시지
- 타임아웃 처리 (5분)
- 재시도 메커니즘

## 🔄 Vue.js에서 React로의 주요 변환점

### 상태 관리
- `reactive()` → `useState()`, `useCallback()`
- `computed()` → `useMemo()`
- `watch()` → `useEffect()`

### 템플릿 문법
- `v-if` → `{condition && <Component />}`
- `v-for` → `{array.map()}`
- `@click` → `onClick`
- `v-model` → `value` + `onChange`

### 컴포넌트 구조
- Single File Components → JSX 파일
- `<template>` → JSX 반환
- `<script setup>` → 함수형 컴포넌트
- `<style scoped>` → CSS 모듈 or styled-components

## 📱 반응형 브레이크포인트

- **Desktop**: > 1200px (2열 레이아웃)
- **Tablet**: 768px - 1200px (1열 레이아웃)
- **Mobile**: < 768px (축약된 탭, 스택 레이아웃)
- **Small Mobile**: < 480px (최소 패딩, 간소화된 UI)

## 🎯 성능 최적화

### React 최적화
- `useCallback`으로 함수 메모이제이션
- `useMemo`로 계산 결과 캐싱
- 조건부 렌더링으로 불필요한 컴포넌트 방지

### 네트워크 최적화
- 이미지 압축 옵션
- API 호출 디바운싱
- 파일 업로드 진행률 표시

### 사용자 경험 개선
- 스켈레톤 로딩
- 에러 바운더리
- 오프라인 감지

## 🔧 커스터마이징

### 색상 테마 변경
`src/styles/App.css`의 CSS 변수를 수정:

```css
:root {
  --primary-color: #00bcd4;      /* 메인 색상 */
  --primary-color-dark: #0097a7;  /* 어두운 메인 색상 */
  --success-color: #4caf50;      /* 성공 색상 */
  --error-color: #f44336;        /* 오류 색상 */
}
```

### 새 탭 추가
1. `components/` 폴더에 새 탭 컴포넌트 생성
2. `ResultTabs.jsx`에 탭 정보 추가
3. `renderTabContent()` 함수에 케이스 추가

## 🧪 테스트

```bash
# 테스트 실행
npm test

# 테스트 커버리지
npm run test:coverage
```

## 📦 배포

### 빌드
```bash
npm run build
```

### 서버 설정
빌드된 파일을 웹 서버에 배포하고, React Router를 위해 모든 경로를 `index.html`로 리다이렉트 설정

## 🤝 기여 방법

1. 포크 생성
2. 피처 브랜치 생성 (`git checkout -b feature/amazing-feature`)
3. 커밋 (`git commit -m 'Add amazing feature'`)
4. 푸시 (`git push origin feature/amazing-feature`)
5. Pull Request 생성

## 📄 라이선스

이 프로젝트는 MIT 라이선스 하에 있습니다.

## 🆘 지원

문제가 발생하면 이슈를 생성해주세요:
- 버그 리포트
- 기능 요청
- 사용 방법 질문

---

**개발팀**: Smart-Eye-by-Friends
**버전**: 2.0.0 (React 18 + Microservices)
**최종 업데이트**: 2025년 9월 17일
**아키텍처**: 마이크로서비스 기반 풀스택 시스템
